# .npmrc

# Lockfile version for consistency
package-lock=true

# Save exact versions to avoid unexpected upgrades
save-exact=true

# Enable stricter dependency resolution
strict-ssl=true

# Optional: Specify registry if using a private one
# registry=https://registry.npmjs.org/